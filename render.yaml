services:
  - type: web
    name: nexus-crm-app
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx tsx server/server.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5001
